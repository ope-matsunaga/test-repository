version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name test-app --s3-location s3://testwork-bucket/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src